#include <avr/io.h>
#include <util/delay.h>

int main(void){
DDRA = 0x00; //Alla pins i A blir till input

//DDRA &= ~(1<<PA3); om det är så man enablar dem till input
//DDRA &= ~(1<<PA4);

  while(1){

    if(PINA & (1<<PA3) == 1) //om resetknappen är nedtryckt{

    }
    
    if(PINA & (1<<PA4) == 1) //om enterknappen är nedtryckt{

  }

}
